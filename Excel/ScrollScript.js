let previousScrollTop = null, previousScrollLeft = null

window.addEventListener("scroll", function(e){
    let currTopScroll = e.currentTarget.scrollTop
    if(previousScrollLeft != e.currentTarget.scrollLeft){
    
    }
    else if(previousScrollTop != e.currentTarget.scrollTop){
    
    }
    })